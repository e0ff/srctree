[owner]
# email = committer-email@gr.ht
# tz = -0800

[agent]
# enabled = false
# upstream_push = false
# upstream_pull = false
# downstream_push = false
# downstream_pull = false
# skip_repos = repo-without-remotes

[server]
# block_scripted_traffic = true
remove_on_start = true
# sock = ./srctree.sock

[repos]
# dir = repos/
# private_dir = private-repos/
# private_repos = private-repo,archived-repo
# unlisted_repos = unlisted-repo

[notifications]
# enabled = false
# Due to a bug in smtp, the email must be in the following format
# sender = "Name" <notif-sender@gr.ht>
# Due to a bug in smtp, the email must be in the following format
# receiver = "Notifer" <email-to@gr.ht>

[git]
push_enabled = false
pull_enabled = true
auto_create_enabled = false
